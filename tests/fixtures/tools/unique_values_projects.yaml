engine: unique_values

dataset:
  name: projects-countries
  description: "Countries with projects"
  source:
    csv: tests/fixtures/data/projects.csv
    url: https://example.org/projects

tool:
  name: project_countries
  description: "Get the list of countries with projects"
  column: COUNTRY
  response: |
    There are {count} countries with projects {filter_label}:
    {list}
    Source: {source}
